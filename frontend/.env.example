# IT-ISEP client (Vite) configuration
# Copy this file to .env and adjust if your backend runs on a different port.
# The dev server proxies /api and /uploads to this target.
VITE_API_TARGET=http://localhost:8081